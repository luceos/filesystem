fof-filesystem:
  admin:
    nav:
      label: Filesystems
      description: Configure filesystems used by extensions.
    button:
      required_package: Packages required
      driver: Select driver
      settings: Configure
      override_settings: Override driver settings
    adapter-settings-modal:
      title: "Settings for {adapter}"
    requested-list: Requests
    adapters-list: Adapters
    adapters:
      aliyun: Aliyun
      aws: AWS
      azure: Azure
      do: Digital Ocean
      dropbox: Dropbox
      ftp: FTP
      gridfs: GridFS
      local: Local
      rackspace: Rackspace
      sftp: SFTP
      webdav: Webdav
    settings:
      local:
        target:
          description: Target directory for storage, relative to Flarum root path.
    requested-driver-modal:
      title: Configure a filesystem for '{requested}'
      adapter: Choose the filesystem
